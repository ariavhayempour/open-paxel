---
description: Batch-analyze Claude Code sessions for repos under the current directory
---

Run `open-paxel upload -y` from the current working directory (or parent if user wants all repos).

Report how many sessions were analyzed and the upload id.
